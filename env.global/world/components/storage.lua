
component.editor = {
	name = "Storage",
	properties = {
		size = {text="size",type="scriptdata",valtype="number",reload=false,key="storage.size"},
		items = {text="items",type="scriptdata",valtype="array",reload=false,key="storage.items"},
		
	},
	
}

function component:OnAttach(node)
	
end
function component:OnDetach(node)
	
end
